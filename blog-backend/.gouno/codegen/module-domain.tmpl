package domain

// {{ camel (arg "name") }} is the root domain type for the {{ snake (arg "name") }} capability.
// Extend it with capability-owned domain state and behavior as the feature is implemented.
type {{ camel (arg "name") }} struct{}
