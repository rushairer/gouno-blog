name: Temporary Wave1 Prettier

on:
  push:
    branches:
      - fix/blog-admin-core-manual-recertification-wave1-20260920

permissions:
  contents: write

jobs:
  format:
    if: "${{ github.actor != 'github-actions[bot]' }}"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout Wave 1
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: fix/blog-admin-core-manual-recertification-wave1-20260920
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: blog-frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: blog-frontend
        run: npm ci

      - name: Apply repository Prettier
        working-directory: blog-frontend
        run: npx prettier --write src/pages/admin/Dashboard.tsx src/pages/admin/Posts.tsx

      - name: Commit formatted files and remove temporary workflow
        run: |
          git rm .github/workflows/tmp-wave1-format.yml
          git add blog-frontend/src/pages/admin/Dashboard.tsx blog-frontend/src/pages/admin/Posts.tsx
          git diff --cached --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "style(ui): apply prettier to Core Wave 1"
          git push origin HEAD:fix/blog-admin-core-manual-recertification-wave1-20260920
