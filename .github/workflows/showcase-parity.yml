name: Blog Showcase Parity

on:
  pull_request:
    paths:
      - "blog-frontend/**"
      - "docs/ui-redesign/SHOWCASE_PARITY_PROTOCOL.md"
      - "docs/ui-redesign/showcase-parity-certifications.json"
      - "docs/ui-redesign/*showcase-parity-hardening.md"
      - "AGENTS.md"
      - ".github/workflows/showcase-parity.yml"
  push:
    branches: [main]
    paths:
      - "blog-frontend/**"
      - "docs/ui-redesign/SHOWCASE_PARITY_PROTOCOL.md"
      - "docs/ui-redesign/showcase-parity-certifications.json"
      - "docs/ui-redesign/*showcase-parity-hardening.md"
      - "AGENTS.md"
      - ".github/workflows/showcase-parity.yml"
  schedule:
    - cron: "17 2 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: blog-showcase-parity-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  parity:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout Blog
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Checkout current Gouno UI main
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: rushairer/gouno-ui
          ref: main
          path: upstream-gouno-ui
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: |
            blog-frontend/package-lock.json
            upstream-gouno-ui/package-lock.json

      - name: Install Blog dependencies
        working-directory: blog-frontend
        run: npm ci

      - name: Install Showcase dependencies
        working-directory: upstream-gouno-ui
        run: npm ci

      - name: Install isolated Playwright runtime
        working-directory: blog-frontend
        run: |
          PLAYWRIGHT_VERSION=1.55.0
          npm install --no-save --ignore-scripts @playwright/test@${PLAYWRIGHT_VERSION}
          npx playwright install --with-deps chromium

      - name: Verify manual-first certification ledger
        working-directory: blog-frontend
        env:
          GOUNO_UI_CANONICAL_ROOT: ../upstream-gouno-ui
          PARITY_BASE_SHA: ${{ github.event.pull_request.base.sha || '' }}
          PARITY_STRICT_HISTORY: "1"
        run: node scripts/check-showcase-parity-certifications.mjs

      - name: Verify AI Settings source parity contract
        working-directory: blog-frontend
        env:
          GOUNO_UI_CANONICAL_ROOT: ../upstream-gouno-ui
        run: node scripts/check-ai-settings-showcase-parity.mjs

      - name: Verify AI Operations source parity contract
        working-directory: blog-frontend
        env:
          GOUNO_UI_CANONICAL_ROOT: ../upstream-gouno-ui
        run: node scripts/check-ai-ops-showcase-parity.mjs

      - name: Compare canonical rendered styles
        working-directory: blog-frontend
        run: npx playwright test --config=e2e/showcase-parity.config.mjs

      - name: Upload paired parity evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: blog-showcase-parity-${{ github.run_id }}
          path: |
            blog-frontend/playwright-report-showcase-parity
            blog-frontend/test-results/showcase-parity
          if-no-files-found: ignore
          retention-days: 14
