name: Migrate Blog Status Pills Once

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/migrate-status-pills-once.yml
      - .github/migrations/status-pills/**

permissions:
  contents: write

concurrency:
  group: migrate-blog-status-pills-once
  cancel-in-progress: false

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: blog-frontend/package-lock.json

      - name: Apply canonical status pill migration and verify
        shell: bash
        run: |
          set -euo pipefail

          cp .github/migrations/status-pills/StatusPill.tsx blog-frontend/src/components/agent/StatusPill.tsx
          cp .github/migrations/status-pills/StatusPill.test.tsx blog-frontend/src/components/agent/__tests__/StatusPill.test.tsx

          node --input-type=module <<'EOF'
          import { readFile, writeFile } from 'node:fs/promises';

          const allowlistPath = 'blog-frontend/scripts/legacy-ui-allowlist.json';
          const targetFile = 'src/components/agent/StatusPill.tsx';
          const symbols = ['RiskBadge', 'StatusIndicator'];
          const allowlist = JSON.parse(await readFile(allowlistPath, 'utf8'));
          allowlist.canonicalSourceSha = '0ba9fabda8e61f3a278b3fb5f37b8f671c167abb';
          let removedPairs = 0;
          for (const symbol of symbols) {
            const files = allowlist.allowedLegacyImports?.[symbol];
            if (!Array.isArray(files) || !files.includes(targetFile)) {
              throw new Error(`Expected ${symbol} legacy pair for ${targetFile}`);
            }
            const next = files.filter((file) => file !== targetFile);
            removedPairs += files.length - next.length;
            if (next.length) allowlist.allowedLegacyImports[symbol] = next;
            else delete allowlist.allowedLegacyImports[symbol];
          }
          if (removedPairs !== 2) {
            throw new Error(`Expected exactly 2 legacy pairs for ${targetFile}, removed ${removedPairs}`);
          }
          await writeFile(allowlistPath, `${JSON.stringify(allowlist, null, 2)}\n`);
          EOF

          cd blog-frontend
          npm ci
          npx prettier --write \
            src/components/agent/StatusPill.tsx \
            src/components/agent/__tests__/StatusPill.test.tsx \
            scripts/legacy-ui-allowlist.json
          npm run test:run -- src/components/agent/__tests__/StatusPill.test.tsx
          npm run quality
          cd ..
          docker build -f blog-frontend/Dockerfile blog-frontend

      - name: Commit verified migration and self-clean
        shell: bash
        run: |
          set -euo pipefail

          if grep -n "@gouno/ui-legacy" blog-frontend/src/components/agent/StatusPill.tsx; then
            echo "StatusPill still imports legacy UI" >&2
            exit 1
          fi
          grep -q '@gouno/ui/core' blog-frontend/src/components/agent/StatusPill.tsx

          rm -rf .github/migrations/status-pills
          rm .github/workflows/migrate-status-pills-once.yml

          git add \
            blog-frontend/src/components/agent/StatusPill.tsx \
            blog-frontend/src/components/agent/__tests__/StatusPill.test.tsx \
            blog-frontend/scripts/legacy-ui-allowlist.json \
            .github/migrations/status-pills \
            .github/workflows/migrate-status-pills-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "refactor(ui): compose Blog status pills from canonical Tag"
          git push origin HEAD:main
