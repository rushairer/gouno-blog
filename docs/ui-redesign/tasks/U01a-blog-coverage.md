# U01a：Blog coverage 阻断处理

状态：verified。前置：阶段 0 已完成盘点。

## 目标与范围

调查 `blog-frontend` `npm run quality` 中 branch coverage 44.58% 低于 45% 的具体缺口。补充高价值 UI/状态测试或修复测试隔离问题，使质量门槛在不降低阈值、不删除断言、不依赖重复运行的情况下通过。只处理可证明属于测试覆盖/稳定性的内容，不提前迁移页面视觉。

## 验收

- [x] 先复现当前 coverage 结果并保存命令/版本基线。
- [x] 说明新增覆盖的业务状态和原因；不为数字添加无意义测试。
- [x] `npm run quality` 完整通过，或明确记录仍受阻原因。
- [x] 更新 `MIGRATION.md` 和本卡结果；未改变 Connector 或后端契约。

## 结果

- 起始 HEAD / 分支：`b1ee1b70e8214ea98140ffda333de76d6276451a` / `main`，工作区干净。
- 根因与改动：分支缺口来自未覆盖公共分类、标签、归档状态及 posts API 参数/空回退分支；新增 7 个有业务意义测试，未改变运行时契约。
- 验证与未测项：`npm run quality` 通过（43 files / 167 tests，覆盖率达到阈值并完成构建）；浏览器验证未执行。
- 下一任务交接：先执行 U01b；U01c 需等待 U01a、U01b 均 verified 后再开始。
