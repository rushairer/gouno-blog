# Q04：第一阶段独立验收

状态：planned。前置：Q01–Q03 已实现且全部改动在当前验收基线；任务卡有结果和 diff/提交定位。

## 目标与范围

在新的会话中复核实际变更，确认测试设施、并发编辑和元信息改动达到验收条件。以代码和可复现结果为准，不只采用实现任务的完成声明。本任务可更新验收记录，不顺手开展第二阶段重构。

## 验收步骤

1. 记录基线 HEAD、各任务变更范围与集成状态；前置不齐先记录缺失，不验收错误版本。
2. 审阅 Q01–Q03 的完整相关 diff，检查隐藏行为变化、版本绕过、数据库隔离、元信息竞争和测试掩盖。
3. 在隔离测试数据库执行当前门禁，确认核心集成用例实际执行。报告成功、失败和 Skip，检查 CI 命令覆盖 CSS。
4. 复核双编辑者冲突、草稿保留、连续保存、未授权写入，以及已定义的其他写入者版本策略。
5. 浏览器检查文章标题、品牌更新、导航与双页编辑。使用本地标准 HTTPS；记录页面版本、视口、操作、截图/DOM和控制台证据。证据放临时或制品目录，不提交用户数据和凭据。
6. 前端质量、后端相关测试/race/vet 通过后停止无意义重复检查；只有新失败或改动才扩展验证。
7. 按严重性给出问题、复现、代码位置和验收影响。非琐碎修复回到对应任务，修复后再验收。已知标题问题不允许用重试绿灯消失。
8. 更新路线图和三张任务卡的 verified/integrated 状态，列出未决项；重新排序 Q05 以后工作并细化下一小批任务及提示词。

## 通过条件

- [ ] Q01–Q03 的每条验收条件均有证据，或明确标记未通过/未验证。
- [ ] 无未解决的阻断项（安全边界退化、数据静默覆盖、必需测试未执行等）。
- [ ] 构建、数据库和页面证据对应同一可识别代码基线。
- [ ] 没有修改 Connector、生产部署或借验收扩大范围。
- [ ] 后续任务已按当前代码调整，尚未决定的渲染方案没有被默认授权。

若任一阻断项存在，本阶段不得标记通过。Q04 通过只代表第一阶段达标，不代表整个项目已到 A+。

## 结果（执行时填写）

- 验收 HEAD 与任务 diff / 提交：未开始
- 检查命令、退出状态、Skip 和环境限制：未执行
- 页面验证与证据位置：无
- 发现（严重性、文件、复现、归属任务）：无
- 结论：未验收
- 任务集成状态及下一批计划：未更新
